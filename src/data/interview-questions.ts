import type { InterviewQuestion } from "./interview";

function q(
  question: string,
  answer: string,
  difficulty: InterviewQuestion["difficulty"],
  tags: string[] = []
): InterviewQuestion {
  return { question, answer, difficulty, tags };
}

export const interviewQuestionBank: Record<string, InterviewQuestion[]> = {
  "llm-fundamentals": [
    q("What is a token?", "A token is the smallest unit an LLM processes. It can be a word, subword, or punctuation. 'ChatGPT' might be 2 tokens. Token count drives API cost and context limits.", "easy", ["tokens"]),
    q("What is the difference between a token and a word?", "Words are linguistic units; tokens are model-specific. One word can be multiple tokens. English averages ~0.75 words per token.", "easy", ["tokens"]),
    q("What is an LLM?", "A Large Language Model is a neural network trained on massive text to predict the next token, enabling text generation, reasoning, and tool use at scale.", "easy", ["llm"]),
    q("GPT vs BERT — key difference?", "GPT is decoder-only (generates left-to-right). BERT is encoder-only (bidirectional understanding). GPT for generation; BERT for classification/embedding tasks.", "medium", ["gpt", "bert"]),
    q("What is autoregressive generation?", "The model generates one token at a time, each conditioned on all previous tokens. Output of step N becomes input for step N+1.", "medium", ["inference"]),
    q("What is a context window?", "Maximum tokens the model can process in one request (input + output). GPT-4o supports 128K; older models had 4K-8K.", "easy", ["context-window"]),
    q("What causes hallucinations?", "Training gaps, probabilistic decoding, no grounding, outdated knowledge. Mitigate with RAG, citations, evals, and human review.", "medium", ["hallucination"]),
    q("What is temperature?", "Scales logits before softmax. 0 = deterministic (argmax). Higher = more random/creative outputs.", "easy", ["temperature"]),
    q("What is top-p (nucleus sampling)?", "Sample from smallest token set whose cumulative probability ≥ p. Dynamically limits vocabulary per step.", "medium", ["top-p"]),
    q("Pre-training vs fine-tuning?", "Pre-training learns general language from web-scale data. Fine-tuning adapts to specific tasks/styles with smaller curated datasets.", "medium", ["fine-tuning"]),
    q("What is RLHF?", "Reinforcement Learning from Human Feedback — train a reward model from human preferences, then optimize the LLM to maximize that reward.", "hard", ["rlhf"]),
    q("What is in-context learning?", "Model learns from examples in the prompt without weight updates. Few-shot prompting leverages this.", "medium", ["prompting"]),
    q("Open vs closed models?", "Open: weights available (Llama, Mistral). Closed: API-only (GPT-4, Claude). Tradeoffs: control/cost vs capability/safety.", "easy", ["models"]),
    q("What is model distillation?", "Train smaller model to mimic larger model outputs. Reduces cost/latency while preserving much of the capability.", "medium", ["distillation"]),
    q("How do you choose an LLM for production?", "Evaluate on your task: accuracy, latency, cost, context length, tool support, data privacy, and vendor lock-in.", "hard", ["production"]),
    q("What is token economics?", "Cost = (input_tokens + output_tokens) × price. Optimize prompts, cache, route simple queries to cheaper models.", "medium", ["cost"]),
    q("What is a system prompt?", "Instructions that set model behavior, tone, constraints. Separated from user input to reduce injection risk.", "easy", ["prompting"]),
    q("What is streaming?", "Tokens returned incrementally as generated. Improves perceived latency for chat UIs.", "easy", ["streaming"]),
    q("What are emergent abilities?", "Capabilities appearing only at scale — chain-of-thought, coding, tool use — not explicitly trained.", "hard", ["emergence"]),
    q("How do LLMs handle long documents?", "Chunking + RAG, summarization, or models with large context windows. Each has cost/quality tradeoffs.", "medium", ["rag", "context-window"]),
  ],

  "prompt-engineering": [
    q("What is prompt engineering?", "Designing inputs (system prompts, examples, format) to get reliable, high-quality LLM outputs without retraining.", "easy", ["prompting"]),
    q("Zero-shot vs few-shot?", "Zero-shot: no examples. Few-shot: 2-5 input-output examples in prompt. Few-shot improves task-specific accuracy.", "easy", ["few-shot"]),
    q("What is chain-of-thought?", "Prompting model to show reasoning steps before the answer. Improves math, logic, and multi-step tasks.", "medium", ["cot"]),
    q("How do you get structured JSON output?", "Use response_format/json_schema (OpenAI), tool calling, or output parsers with validation and retry.", "medium", ["structured-output"]),
    q("What is prompt injection?", "Malicious input overriding system instructions. Defense: input sanitization, privilege separation, output filtering.", "hard", ["security"]),
    q("Best temperature for extraction tasks?", "0 or near 0 — deterministic, factual outputs. Avoid high temperature for classification/extraction.", "easy", ["temperature"]),
    q("How do you write a good system prompt?", "Define role, constraints, output format, edge cases, and what NOT to do. Be specific, not verbose.", "medium", ["system-prompt"]),
    q("What is prompt chaining?", "Breaking complex tasks into sequential prompts where each output feeds the next. Improves reliability.", "medium", ["chaining"]),
    q("What is self-consistency?", "Generate multiple answers, pick majority vote. Improves reasoning accuracy at higher cost.", "hard", ["reasoning"]),
    q("How do you handle long prompts?", "Summarize, retrieve only relevant context (RAG), use prompt compression, or models with larger context.", "medium", ["context"]),
    q("What is a prompt template?", "Reusable prompt with variables (e.g. {user_query}). Enables versioning and A/B testing.", "easy", ["templates"]),
    q("Common prompt engineering mistakes?", "Vague instructions, no format spec, wrong temperature, no edge case handling, no output validation.", "medium", ["mistakes"]),
    q("How do you evaluate prompts?", "Hold-out test set, human eval, automated metrics (accuracy, faithfulness), A/B testing in production.", "medium", ["evaluation"]),
    q("What is role prompting?", "Assigning a persona ('You are a senior Python engineer') to shape tone and expertise level.", "easy", ["role"]),
    q("Delimiter usage in prompts?", "Use XML tags or markdown sections to separate instructions, context, and user input — reduces confusion.", "medium", ["formatting"]),
    q("How to reduce hallucination via prompting?", "Ask model to cite sources, say 'I don't know' if unsure, provide context via RAG, use lower temperature.", "medium", ["hallucination"]),
    q("What is meta-prompting?", "Using an LLM to generate or improve prompts. Useful for optimization but needs human review.", "hard", ["meta"]),
    q("Prompt versioning in production?", "Store prompts in git/DB, version with tags, track metrics per version, rollback on regression.", "medium", ["production"]),
    q("How do guardrails relate to prompts?", "Guardrails enforce policies beyond the prompt — input/output filters, topic restrictions, PII redaction.", "medium", ["guardrails"]),
    q("When is prompt engineering not enough?", "When you need consistent behavior change (fine-tune), private knowledge (RAG), or domain expertise at scale.", "medium", ["fine-tuning", "rag"]),
  ],

  "transformers": [
    q("What problem did transformers solve?", "RNNs processed sequentially (slow, vanishing gradients). Transformers use parallel self-attention for long-range dependencies.", "medium", ["transformers"]),
    q("Explain self-attention intuitively.", "Each token computes how much to 'attend to' every other token. Produces context-aware representations.", "medium", ["attention"]),
    q("What are Q, K, V?", "Query, Key, Value vectors. Attention weights = softmax(QK^T/√d). Output = weighted sum of Values.", "hard", ["attention"]),
    q("Why multi-head attention?", "Multiple attention heads capture different relationship types (syntax, semantics) in parallel.", "medium", ["attention"]),
    q("Encoder vs decoder?", "Encoder: bidirectional, understands input. Decoder: causal/masked, generates output autoregressively.", "medium", ["architecture"]),
    q("What is positional encoding?", "Adds position info since attention is permutation-invariant. Sinusoidal (original) or learned (modern).", "medium", ["positional"]),
    q("What is layer normalization?", "Normalizes activations per layer for training stability. Used in every transformer block.", "medium", ["training"]),
    q("Residual connections — why?", "Allow gradients to flow through deep networks. output = sublayer(x) + x.", "medium", ["architecture"]),
    q("Masked attention in decoders?", "Token i can only attend to tokens ≤ i. Prevents cheating by seeing future tokens during training.", "medium", ["decoder"]),
    q("Transformer block components?", "Multi-head attention → Add & Norm → Feed-forward (2 linear + activation) → Add & Norm.", "medium", ["architecture"]),
    q("BERT vs GPT architecture?", "BERT: encoder-only, MLM pre-training. GPT: decoder-only, next-token prediction.", "medium", ["bert", "gpt"]),
    q("What is RoPE?", "Rotary Position Embedding — encodes relative position via rotation in embedding space. Used in Llama, Mistral.", "hard", ["rope"]),
    q("What is KV cache?", "Caches key/value tensors from prior tokens during inference. Avoids recomputation, speeds generation.", "medium", ["kv-cache"]),
    q("Attention complexity?", "O(n²) in sequence length — quadratic cost for long contexts. Flash Attention optimizes memory.", "hard", ["complexity"]),
    q("Cross-attention vs self-attention?", "Self: within same sequence. Cross: decoder attends to encoder output (seq2seq models).", "medium", ["attention"]),
    q("Why transformers dominate NLP?", "Parallelizable, scalable, captures long-range deps, transfer learning via pre-training.", "easy", ["transformers"]),
    q("What is Flash Attention?", "IO-aware attention algorithm reducing memory from O(n²) to O(n). Enables longer contexts on same GPU.", "hard", ["optimization"]),
    q("Feed-forward network in transformers?", "Two linear layers with activation (GELU/ReLU) between attention blocks. Typically 4× hidden dim expansion.", "medium", ["ffn"]),
    q("How does scaling laws apply?", "Performance improves predictably with more parameters, data, and compute. Guides model sizing decisions.", "hard", ["scaling"]),
    q("Draw transformer architecture for interview.", "Input → Embed + Position → N×(MHA → FFN) → Output projection. Mention residual + layer norm each block.", "medium", ["architecture"]),
  ],

  "neural-networks": [
    q("What is a neural network?", "Layers of neurons with weights and biases, connected by activation functions. Learns by adjusting weights via gradient descent.", "easy", ["basics"]),
    q("What is gradient descent?", "Iteratively update weights in direction that reduces loss. w = w - lr × ∂loss/∂w.", "easy", ["optimization"]),
    q("What is backpropagation?", "Chain rule applied to compute gradients through the network, layer by layer, from output to input.", "medium", ["backprop"]),
    q("Common activation functions?", "ReLU (most common), GELU (transformers), Sigmoid (binary output), Softmax (multi-class probabilities).", "medium", ["activation"]),
    q("What is a loss function?", "Measures prediction error. MSE for regression, cross-entropy for classification. Training minimizes this.", "easy", ["loss"]),
    q("What is overfitting?", "Model memorizes training data, poor on new data. Fix: more data, regularization, dropout, early stopping.", "easy", ["overfitting"]),
    q("What is dropout?", "Randomly zero neurons during training. Prevents co-adaptation, acts as regularization.", "medium", ["regularization"]),
    q("Learning rate — too high vs too low?", "Too high: diverges, loss spikes. Too low: slow convergence, stuck in local minima. Use schedulers.", "medium", ["learning-rate"]),
    q("What is batch normalization?", "Normalizes layer inputs across batch. Stabilizes training, allows higher learning rates.", "medium", ["batch-norm"]),
    q("Adam optimizer?", "Adaptive learning rate per parameter using momentum + RMSprop. Default choice for most deep learning.", "medium", ["optimizer"]),
    q("What is an epoch?", "One full pass through the entire training dataset.", "easy", ["training"]),
    q("Vanishing gradient problem?", "Gradients shrink in deep networks (sigmoid/tanh). Solved by ReLU, residual connections, better init.", "medium", ["gradients"]),
    q("What is transfer learning?", "Use pre-trained model weights as starting point. Fine-tune on your task with less data/compute.", "medium", ["transfer"]),
    q("Parameters vs hyperparameters?", "Parameters: learned weights. Hyperparameters: set before training (lr, layers, batch size).", "easy", ["basics"]),
    q("What is a weight initialization?", "Starting weight values affect training. Xavier/He init prevents vanishing/exploding gradients.", "medium", ["init"]),
    q("Forward pass vs backward pass?", "Forward: compute predictions. Backward: compute gradients via backprop for weight updates.", "easy", ["training"]),
    q("What is regularization?", "Techniques to prevent overfitting: L1/L2 penalty, dropout, data augmentation, early stopping.", "medium", ["regularization"]),
    q("How many layers is 'deep'?", "No fixed number — generally 3+ hidden layers. Transformers have 12-96+ layers.", "easy", ["depth"]),
    q("GPU vs CPU for neural networks?", "GPUs parallelize matrix ops (core of NN compute). 10-100× faster for training/inference.", "easy", ["hardware"]),
    q("Explain neural network to non-technical person.", "A system that learns patterns from examples by adjusting internal knobs — like tuning a recipe until the output matches what you want.", "easy", ["communication"]),
  ],

  "llm-apis": [
    q("How do you call OpenAI API?", "Install openai SDK, set API key, use client.chat.completions.create with model, messages, and parameters.", "easy", ["openai"]),
    q("messages array structure?", "List of {role: system|user|assistant, content: string}. Maintains conversation history.", "easy", ["api"]),
    q("How to handle API rate limits?", "Exponential backoff, request queuing, caching, batch where possible, upgrade tier, use multiple keys.", "medium", ["rate-limits"]),
    q("Streaming API implementation?", "Set stream=True, iterate over chunks, yield tokens to UI as they arrive.", "easy", ["streaming"]),
    q("Function calling flow?", "Define tools in API call → model returns tool_calls → execute function → send result back → model continues.", "medium", ["tools"]),
    q("How to reduce API costs?", "Shorter prompts, cheaper models for simple tasks, caching, batch API, local models for dev.", "medium", ["cost"]),
    q("Error handling best practices?", "Retry transient errors (429, 500), timeout handling, fallback models, log all failures.", "medium", ["errors"]),
    q("Structured output with OpenAI?", "response_format: {type: 'json_schema', json_schema: {...}}. Enforces valid JSON matching schema.", "medium", ["structured"]),
    q("Embeddings API use case?", "Convert text to vectors for semantic search, clustering, RAG retrieval.", "easy", ["embeddings"]),
    q("How to switch between providers?", "Abstract behind interface (LangChain, LiteLLM). Same code, different model strings/API keys.", "medium", ["abstraction"]),
    q("Ollama for local development?", "Run open models locally. Same API patterns, zero API cost, good for prototyping.", "easy", ["ollama"]),
    q("Token counting before API call?", "Use tiktoken library to estimate tokens and cost before sending. Prevent context overflow.", "medium", ["tokens"]),
    q("Async API calls?", "Use asyncio + async client for concurrent requests. Critical for agent systems with parallel tool calls.", "medium", ["async"]),
    q("API key security?", "Never in frontend/client code. Use env vars, secrets manager, proxy server for browser apps.", "easy", ["security"]),
    q("Conversation memory management?", "Truncate old messages, summarize history, or store in vector DB when exceeding context window.", "medium", ["memory"]),
    q("Batch API for cost savings?", "OpenAI Batch API: 50% discount, 24h turnaround. Good for offline processing.", "medium", ["batch"]),
    q("Vision API usage?", "Send image as base64 or URL in message content array. Model processes image + text together.", "medium", ["vision"]),
    q("How to test without API costs?", "Use mock responses, smaller models, Ollama locally, or recorded fixtures.", "easy", ["testing"]),
    q("Latency optimization?", "Streaming, edge deployment, model routing (fast model first), prompt compression, caching.", "medium", ["latency"]),
    q("Multi-provider fallback?", "Primary: GPT-4. Fallback: Claude/Gemini on failure. Circuit breaker pattern for reliability.", "hard", ["reliability"]),
  ],

  "rag-system-design": [
    q("Design RAG for 1M documents.", "Ingest pipeline → chunk (512 tok) → embed → Pinecone/Qdrant → hybrid search → rerank → generate with citations → RAGAS eval.", "hard", ["system-design"]),
    q("When RAG fails?", "Poor chunking, wrong embedding model, no reranking, stale index, query-document mismatch. Debug retrieval separately.", "medium", ["debugging"]),
    q("Chunking strategies?", "Fixed-size, recursive (by headers), semantic (by meaning). Use 10-20% overlap. Match strategy to document type.", "medium", ["chunking"]),
    q("Hybrid search architecture?", "BM25 (keyword) + dense retrieval (semantic). Merge scores with weighted fusion or RRF.", "medium", ["hybrid"]),
    q("When to re-rank?", "Always in production for top-k > 5. Cross-encoder reranker dramatically improves precision.", "medium", ["reranking"]),
    q("Metadata filtering in RAG?", "Tag chunks with source, date, department. Filter before vector search for scoped retrieval.", "medium", ["metadata"]),
    q("RAG evaluation metrics?", "Retrieval: precision@k, recall@k. Generation: faithfulness, answer relevance, context precision (RAGAS).", "medium", ["evaluation"]),
    q("Caching in RAG?", "Cache embeddings, frequent queries, and LLM responses. Redis with TTL. Huge cost savings.", "medium", ["caching"]),
    q("Multi-tenant RAG?", "Namespace per tenant in vector DB, metadata isolation, separate indexes or filtered queries.", "hard", ["multi-tenant"]),
    q("Document update strategy?", "Incremental indexing: detect changes, re-embed only modified chunks, delete stale vectors.", "medium", ["indexing"]),
    q("RAG vs fine-tuning decision?", "RAG: dynamic knowledge, citations. Fine-tune: behavior/style. Often combine both.", "medium", ["decision"]),
    q("Handling tables and images in RAG?", "Specialized parsers (Unstructured, LlamaParse), OCR, table-to-markdown, multimodal embeddings.", "hard", ["multimodal"]),
    q("Query transformation?", "Rewrite, expand, or decompose user queries before retrieval. HyDE generates hypothetical document.", "medium", ["query"]),
    q("Parent-child chunking?", "Small chunks for retrieval, return larger parent chunk for context. Best of precision + context.", "medium", ["chunking"]),
    q("RAG latency budget?", "Retrieval <200ms, rerank <100ms, generation streaming. Total P95 <3s for good UX.", "medium", ["latency"]),
    q("Security in enterprise RAG?", "Access control on documents, audit logs, PII detection, prompt injection defense in retrieved content.", "hard", ["security"]),
    q("Feedback loop in RAG?", "Collect thumbs up/down, use for eval set, identify bad retrievals, retrain reranker.", "medium", ["feedback"]),
    q("Vector DB selection criteria?", "Scale, latency, filtering, hybrid search support, managed vs self-hosted, cost.", "medium", ["vector-db"]),
    q("Context compression?", "Summarize retrieved chunks before sending to LLM. Saves tokens, may lose detail.", "medium", ["compression"]),
    q("Agentic RAG?", "Agent decides when to retrieve, what to search, whether answer is sufficient. More flexible than fixed pipeline.", "hard", ["agents"]),
  ],

  "vector-search": [
    q("What is an embedding?", "Dense vector representation capturing semantic meaning. Similar texts → nearby vectors.", "easy", ["embeddings"]),
    q("Cosine similarity vs dot product?", "Cosine: angle between vectors (normalized). Dot product: magnitude matters. Use cosine for text embeddings.", "medium", ["similarity"]),
    q("What is ANN search?", "Approximate Nearest Neighbor — trades perfect accuracy for speed. HNSW, IVF algorithms.", "medium", ["ann"]),
    q("HNSW explained?", "Hierarchical Navigable Small World graph. Multi-layer graph for fast approximate nearest neighbor search.", "hard", ["hnsw"]),
    q("Embedding model selection?", "Match domain (general vs code vs medical). Benchmark on your data. OpenAI, Cohere, BGE, E5 popular.", "medium", ["models"]),
    q("Vector dimension tradeoffs?", "Higher dim = more expressive but slower search, more storage. 384-1536 common range.", "medium", ["dimensions"]),
    q("What is BM25?", "Keyword-based ranking using term frequency and inverse document frequency. Complements semantic search.", "medium", ["bm25"]),
    q("When does vector search fail?", "Exact keyword matches (IDs, codes), rare terms, domain mismatch between query and corpus embedding model.", "medium", ["limitations"]),
    q("Normalize embeddings?", "L2 normalize so cosine similarity = dot product. Standard practice for most embedding models.", "easy", ["normalization"]),
    q("Index rebuild strategy?", "Full rebuild for model changes. Incremental for new documents. Plan downtime or blue-green.", "medium", ["ops"]),
    q("Pinecone vs Qdrant vs Chroma?", "Pinecone: managed, scalable. Qdrant: self-hosted, filtering. Chroma: dev-friendly, local. Match to scale.", "medium", ["tools"]),
    q("What is a cross-encoder?", "Jointly encodes query+document for relevance score. Slow but accurate — use for reranking top-k.", "medium", ["reranking"]),
    q("Bi-encoder vs cross-encoder?", "Bi-encoder: embed separately, fast retrieval. Cross-encoder: encode together, slow but precise reranking.", "medium", ["architecture"]),
    q("Embedding drift?", "Model updates change vector space. Re-embed entire corpus when switching embedding models.", "medium", ["maintenance"]),
    q("Multi-vector retrieval?", "ColBERT: multiple vectors per document. Better granularity, more storage.", "hard", ["colbert"]),
    q("Filtered vector search?", "Apply metadata filters (date, category) before/during ANN search. Critical for enterprise.", "medium", ["filtering"]),
    q("Embedding batch size?", "Batch embed for throughput. OpenAI supports up to 2048 inputs per call. Balance latency vs throughput.", "easy", ["batching"]),
    q("Semantic cache?", "Embed query, find similar past queries in cache, return cached response. Saves LLM calls.", "medium", ["caching"]),
    q("Evaluate embedding quality?", "Retrieval benchmarks (MRR, nDCG), cluster visualization, manual inspection of nearest neighbors.", "medium", ["evaluation"]),
    q("Sparse vs dense retrieval?", "Sparse (BM25): keyword matching. Dense (embeddings): semantic matching. Hybrid combines both.", "medium", ["hybrid"]),
  ],

  "agent-architecture": [
    q("What is an AI agent?", "LLM-powered system that observes, reasons, acts via tools, and loops until task completion.", "easy", ["agents"]),
    q("Agent loop components?", "Observe state → LLM reasoning → tool selection → execute → update memory → repeat.", "easy", ["loop"]),
    q("Single vs multi-agent?", "Single: simpler, one LLM. Multi: specialized roles (researcher, writer, critic), better for complex tasks.", "medium", ["multi-agent"]),
    q("How to prevent infinite loops?", "Max iterations, timeout, cost budget, duplicate action detection, human escalation.", "medium", ["safety"]),
    q("Agent memory tiers?", "Short-term (context window), working (summaries), long-term (vector store). Consolidate over time.", "medium", ["memory"]),
    q("Tool design for agents?", "Clear descriptions, typed parameters, idempotent when possible, return structured errors.", "medium", ["tools"]),
    q("Human-in-the-loop patterns?", "Approval gates for destructive actions, escalation on low confidence, review before send.", "medium", ["hitl"]),
    q("Agent observability?", "Trace every step: LLM calls, tool invocations, latencies, token usage. LangSmith, Phoenix.", "medium", ["observability"]),
    q("Error recovery in agents?", "Retry with backoff, alternative tools, self-correction prompts, graceful degradation.", "medium", ["errors"]),
    q("State management?", "Maintain agent state object: conversation, tool results, plan progress. Persist for long-running agents.", "medium", ["state"]),
    q("Agent evaluation?", "Task completion rate, steps to completion, cost per task, human satisfaction scores.", "medium", ["evaluation"]),
    q("Autonomy levels?", "Level 1: suggest only. Level 2: act with approval. Level 3: fully autonomous within guardrails.", "medium", ["autonomy"]),
    q("Agent security concerns?", "Tool privilege escalation, data exfiltration, prompt injection via tools, unauthorized API access.", "hard", ["security"]),
    q("Planning in agents?", "Explicit plan generation before execution. Helps complex tasks but less adaptive than ReAct.", "medium", ["planning"]),
    q("Sub-agent delegation?", "Parent agent spawns child agents for subtasks. CrewAI, AutoGen patterns.", "hard", ["delegation"]),
    q("Agent deployment?", "Containerized, stateless where possible, queue for long tasks, webhook for async completion.", "medium", ["deployment"]),
    q("Cost control for agents?", "Token budgets per task, cheaper models for planning, cache tool results, limit iterations.", "medium", ["cost"]),
    q("When NOT to use agents?", "Simple Q&A (use RAG), deterministic workflows (use code), latency-critical paths.", "medium", ["decision"]),
    q("Agent framework selection?", "LangGraph (graphs), CrewAI (roles), OpenAI Agents SDK (simple), pick based on complexity needs.", "medium", ["frameworks"]),
    q("Design customer support agent.", "Intent classification → knowledge retrieval → response generation → escalation detection → CRM integration → conversation memory.", "hard", ["system-design"]),
  ],

  "react-pattern": [
    q("What is ReAct?", "Reasoning + Acting — interleave thought, action, observation steps until task complete.", "easy", ["react"]),
    q("ReAct prompt format?", "Thought: [reasoning] → Action: [tool] → Action Input: [args] → Observation: [result] → repeat.", "medium", ["format"]),
    q("ReAct vs chain-of-thought?", "CoT: reasoning only, no tools. ReAct: reasoning + tool use + observation loop.", "easy", ["comparison"]),
    q("ReAct advantages?", "Adaptive to unexpected tool results, interpretable trace, handles multi-step tasks.", "medium", ["advantages"]),
    q("ReAct limitations?", "Can be slow (many LLM calls), may loop, sensitive to tool description quality.", "medium", ["limitations"]),
    q("Plan-and-Execute vs ReAct?", "Plan-Execute: plan all steps first. ReAct: adapt per step. ReAct better for exploration.", "medium", ["comparison"]),
    q("Tree of Thoughts?", "Explore multiple reasoning branches, evaluate, prune. Better for complex problems, higher cost.", "hard", ["tot"]),
    q("Reflexion pattern?", "Agent reflects on failures, updates strategy, retries. Self-improvement loop.", "hard", ["reflexion"]),
    q("How to implement ReAct from scratch?", "Loop: call LLM with tools → parse thought/action → execute tool → append observation → repeat.", "medium", ["implementation"]),
    q("Parsing ReAct output?", "Regex or structured output for Thought/Action/Observation. Handle malformed outputs with retry.", "medium", ["parsing"]),
    q("ReAct with multiple tools?", "Provide all tool schemas. Model selects one per step. Clear descriptions critical.", "medium", ["tools"]),
    q("When does ReAct fail?", "Ambiguous tools, too many tools, poor observations, no max iteration limit.", "medium", ["failures"]),
    q("Self-correction in ReAct?", "After observation, model can recognize error and try different action.", "medium", ["correction"]),
    q("ReAct for coding agents?", "Thought: analyze code → Action: read_file → Observation: file contents → Action: edit_file.", "medium", ["coding"]),
    q("Optimizing ReAct latency?", "Parallel tool calls, cache observations, smaller model for simple steps.", "medium", ["latency"]),
    q("ReAct evaluation metrics?", "Task success rate, average steps, tool call accuracy, total tokens used.", "medium", ["evaluation"]),
    q("LangGraph for ReAct?", "Model agent as state graph: nodes = steps, edges = conditions. Better control than raw loops.", "medium", ["langgraph"]),
    q("Memory in ReAct agents?", "Full trace in context. Summarize old steps when context fills up.", "medium", ["memory"]),
    q("ReAct paper key insight?", "Interleaving reasoning and acting outperforms reasoning-only or acting-only approaches.", "medium", ["paper"]),
    q("Production ReAct checklist?", "Max iterations, timeouts, tool auth, logging, cost limits, human approval for sensitive actions.", "hard", ["production"]),
  ],

  "langgraph": [
    q("What is LangGraph?", "Library for building stateful, multi-step agent workflows as graphs. Built on LangChain.", "easy", ["langgraph"]),
    q("Graph vs chain?", "Chains: linear. Graphs: cycles, branches, conditional edges. Agents need cycles.", "medium", ["graphs"]),
    q("State in LangGraph?", "Typed state object passed between nodes. Each node reads/updates state.", "medium", ["state"]),
    q("Nodes and edges?", "Nodes: functions (LLM call, tool). Edges: transitions. Conditional edges: routing logic.", "medium", ["architecture"]),
    q("Human-in-the-loop in LangGraph?", "Interrupt before/after nodes. Pause for approval, resume with Command.", "medium", ["hitl"]),
    q("Checkpointing?", "Save graph state at each step. Enable resume, replay, time-travel debugging.", "medium", ["checkpointing"]),
    q("Subgraphs?", "Nest graphs within graphs. Reusable agent components.", "medium", ["subgraphs"]),
    q("LangGraph vs raw agent loop?", "Graph: visual, debuggable, persistent state. Raw loop: simpler for basic cases.", "medium", ["comparison"]),
    q("Streaming in LangGraph?", "Stream node outputs and state updates. astream_events for detailed traces.", "medium", ["streaming"]),
    q("Error handling?", "Try/except in nodes, conditional edges to error handler node, retry policies.", "medium", ["errors"]),
    q("Parallel nodes?", "Fan-out to multiple nodes, fan-in to merge. For parallel tool calls.", "medium", ["parallel"]),
    q("LangGraph Studio?", "Visual IDE for building and debugging agent graphs.", "easy", ["tools"]),
    q("Persistence backends?", "Memory, SQLite, Postgres for checkpoint storage. Postgres for production.", "medium", ["persistence"]),
    q("Testing LangGraph agents?", "Unit test nodes, integration test full graph, mock LLM responses.", "medium", ["testing"]),
    q("Deployment?", "Package as API, use LangGraph Platform or self-host with FastAPI.", "medium", ["deployment"]),
    q("Common patterns?", "ReAct agent, plan-execute, supervisor (multi-agent), evaluator-optimizer.", "medium", ["patterns"]),
    q("State reducers?", "Define how state updates merge (append, replace, custom). Critical for message history.", "hard", ["reducers"]),
    q("When to use LangGraph?", "Complex agent flows, need persistence, human approval, multi-agent orchestration.", "medium", ["decision"]),
    q("LangGraph memory?", "Built-in checkpointing + external vector store for long-term memory.", "medium", ["memory"]),
    q("Performance tips?", "Minimize state size, cache LLM calls, use async nodes, batch where possible.", "medium", ["performance"]),
  ],

  "mcp-design": [
    q("What is MCP?", "Model Context Protocol — open standard connecting AI models to tools, resources, and data via client-server architecture.", "easy", ["mcp"]),
    q("MCP architecture?", "Host (IDE/app) → MCP Client → MCP Server → Tools/Resources. JSON-RPC transport.", "medium", ["architecture"]),
    q("Tools vs Resources in MCP?", "Tools: actions model can invoke. Resources: data model can read (files, DB records).", "medium", ["concepts"]),
    q("MCP vs function calling?", "MCP: standardized, discoverable, reusable across clients. Function calling: ad-hoc per API.", "medium", ["comparison"]),
    q("Building an MCP server?", "Implement tool/resource handlers, use MCP SDK, expose via stdio or HTTP transport.", "medium", ["implementation"]),
    q("MCP transport options?", "stdio (local), HTTP/SSE (remote). stdio for IDE plugins, HTTP for cloud services.", "medium", ["transport"]),
    q("MCP security?", "Authentication, authorization per tool, sandbox execution, audit logging.", "hard", ["security"]),
    q("MCP prompts?", "Pre-built prompt templates servers can expose. Client presents to model.", "medium", ["prompts"]),
    q("Use case: GitHub MCP?", "Expose repos, issues, PRs as tools. Agent can search code, create issues, review PRs.", "medium", ["example"]),
    q("Use case: Database MCP?", "Expose schema as resources, queries as tools. Agent explores and queries DB safely.", "medium", ["example"]),
    q("MCP client implementation?", "Connect to server, discover capabilities, route model tool calls to MCP tools.", "medium", ["client"]),
    q("Multiple MCP servers?", "Client connects to many servers. Model sees unified tool namespace.", "medium", ["multi-server"]),
    q("MCP in production?", "Auth tokens, rate limiting, monitoring, version management, graceful degradation.", "hard", ["production"]),
    q("Testing MCP servers?", "Unit test handlers, integration test with MCP client, mock external APIs.", "medium", ["testing"]),
    q("MCP vs plugins?", "MCP: open protocol, cross-platform. Plugins: vendor-specific (ChatGPT plugins). MCP more portable.", "medium", ["comparison"]),
    q("Error handling in MCP?", "Structured error responses, tool-level timeouts, circuit breakers for external deps.", "medium", ["errors"]),
    q("MCP discovery?", "Client calls list_tools/list_resources on connect. Dynamic capability discovery.", "easy", ["discovery"]),
    q("Design filesystem MCP.", "Tools: read_file, write_file, list_dir. Resources: file tree. Auth: path sandboxing.", "medium", ["system-design"]),
    q("MCP adoption benefits?", "Standardized integrations, ecosystem of servers, reduced custom connector code.", "easy", ["benefits"]),
    q("Future of MCP?", "Industry standard for agent tool connectivity. Growing server ecosystem (GitHub, Slack, DBs).", "easy", ["future"]),
  ],

  "production-ai": [
    q("Design production LLM application.", "API gateway → prompt management → model routing → caching → monitoring → eval pipeline → fallback.", "hard", ["system-design"]),
    q("LLM gateway pattern?", "Central proxy for all LLM calls. Handles auth, rate limiting, logging, model routing, cost tracking.", "medium", ["gateway"]),
    q("Model routing?", "Route simple queries to cheap/fast models, complex to capable models. Classifier or rules-based.", "medium", ["routing"]),
    q("Caching strategies?", "Exact match cache, semantic cache (embedding similarity), prompt cache (prefix caching).", "medium", ["caching"]),
    q("SLA for AI apps?", "Define P50/P95 latency, availability, accuracy targets. Monitor and alert on breaches.", "medium", ["sla"]),
    q("A/B testing prompts?", "Split traffic, measure quality metrics, statistical significance, gradual rollout.", "medium", ["testing"]),
    q("Deployment strategies?", "Blue-green, canary releases, feature flags for prompt versions.", "medium", ["deployment"]),
    q("Handling model updates?", "Version pin in production, eval before upgrade, rollback plan, shadow testing.", "medium", ["ops"]),
    q("Multi-region deployment?", "Deploy inference close to users, replicate vector DB, handle data residency.", "hard", ["scaling"]),
    q("Cost monitoring?", "Track tokens per user/feature, set budgets, alerts on anomalies, monthly cost reports.", "medium", ["cost"]),
    q("Fallback chains?", "GPT-4 → GPT-4o-mini → cached response → graceful error message.", "medium", ["reliability"]),
    q("Data privacy in production?", "PII detection, data retention policies, no training on user data, encryption.", "hard", ["privacy"]),
    q("Load testing AI apps?", "Simulate concurrent users, measure latency under load, identify bottlenecks (retrieval, LLM).", "medium", ["testing"]),
    q("Feature flags for AI?", "Toggle prompts, models, RAG pipelines without deployment. Quick rollback.", "medium", ["feature-flags"]),
    q("Compliance (SOC2, GDPR)?", "Audit logs, data processing agreements, right to deletion, access controls.", "hard", ["compliance"]),
    q("On-prem vs cloud LLM?", "On-prem: data control, higher ops cost. Cloud: easier, better models. Hybrid common.", "medium", ["deployment"]),
    q("Incident response for AI?", "Detect (monitoring), contain (disable feature), diagnose (traces), fix, postmortem.", "medium", ["incidents"]),
    q("Scaling vector DB?", "Sharding, replicas, managed services. Plan for 10x growth in document count.", "medium", ["scaling"]),
    q("Production checklist?", "Monitoring, evals, caching, fallbacks, rate limits, security, cost controls, documentation.", "medium", ["checklist"]),
    q("Build vs buy for AI infra?", "Buy: vector DB, observability. Build: business logic, prompts, evals. Use managed where possible.", "medium", ["decision"]),
  ],

  "ai-observability": [
    q("Why observability for AI?", "LLM outputs are non-deterministic. Need tracing, eval, and monitoring to debug and improve.", "easy", ["observability"]),
    q("What to trace?", "Every LLM call, retrieval, tool invocation, latency, tokens, cost, user feedback.", "medium", ["tracing"]),
    q("LangSmith vs Phoenix?", "LangSmith: LangChain-native, production-ready. Phoenix: open-source, Arize. Both do tracing + eval.", "medium", ["tools"]),
    q("Key metrics?", "Latency P50/P95, token usage, error rate, user satisfaction, retrieval precision, cost per query.", "medium", ["metrics"]),
    q("Distributed tracing for agents?", "Span per agent step, parent-child relationships, trace full multi-step execution.", "medium", ["tracing"]),
    q("Logging best practices?", "Structured JSON logs, correlation IDs, log prompts + responses (redact PII), retention policy.", "medium", ["logging"]),
    q("Alerting on AI metrics?", "Alert on: error rate spike, latency P95 > threshold, cost anomaly, eval score drop.", "medium", ["alerting"]),
    q("Dashboard design?", "Real-time: latency, errors, cost. Daily: quality metrics, user feedback, top failures.", "medium", ["dashboards"]),
    q("Eval in production?", "Continuous eval on sample traffic, regression detection, human review queue.", "medium", ["evaluation"]),
    q("OpenTelemetry for LLM?", "Standard spans for LLM calls. Vendor-agnostic tracing. Growing ecosystem support.", "medium", ["opentelemetry"]),
    q("Debugging bad responses?", "Trace: retrieval results → prompt sent → model response. Identify failure point.", "medium", ["debugging"]),
    q("Cost attribution?", "Tag traces with user, feature, team. Aggregate costs for chargeback.", "medium", ["cost"]),
    q("Feedback collection?", "Thumbs up/down, explicit ratings, implicit signals (copy, regenerate). Feed into eval.", "medium", ["feedback"]),
    q("Prompt regression detection?", "Automated eval on prompt change. Block deploy if scores drop below threshold.", "medium", ["regression"]),
    q("Sampling strategy?", "Trace 100% in dev, sample 10-20% in prod. Always trace errors.", "medium", ["sampling"]),
    q("PII in traces?", "Redact before storage. Hash user IDs. Comply with data retention policies.", "hard", ["privacy"]),
    q("Observability for RAG?", "Log: query, retrieved chunks (with scores), generated answer, citations, user feedback.", "medium", ["rag"]),
    q("SLO for AI services?", "99.9% availability, P95 latency <2s, eval score >0.85. Define per feature.", "medium", ["slo"]),
    q("Tools stack?", "LangSmith/Phoenix (tracing), Grafana (dashboards), PagerDuty (alerts), custom eval pipeline.", "medium", ["stack"]),
    q("Building eval pipeline?", "Test set → automated metrics → human review → regression alerts → continuous improvement.", "hard", ["pipeline"]),
  ],

  "fine-tuning": [
    q("What is fine-tuning?", "Continued training of a pre-trained model on task-specific data to adapt behavior.", "easy", ["fine-tuning"]),
    q("Fine-tuning vs RAG?", "Fine-tune: change behavior/style. RAG: add knowledge. Often used together.", "easy", ["comparison"]),
    q("When to fine-tune?", "Consistent format needed, domain-specific language, classification, when prompting isn't reliable enough.", "medium", ["decision"]),
    q("What is LoRA?", "Low-Rank Adaptation — train small adapter matrices instead of full weights. 99% fewer trainable params.", "medium", ["lora"]),
    q("LoRA vs full fine-tune?", "LoRA: cheaper, faster, multiple adapters per base model. Full: maximum adaptation, expensive.", "medium", ["lora"]),
    q("What is QLoRA?", "LoRA + 4-bit quantized base model. Fine-tune large models on consumer GPUs.", "hard", ["qlora"]),
    q("Training data format?", "JSONL with instruction/input/output or conversation format. Quality > quantity.", "medium", ["data"]),
    q("How much data needed?", "100-1000 high-quality examples often sufficient for LoRA. More for full fine-tune.", "medium", ["data"]),
    q("Evaluating fine-tuned model?", "Hold-out test set, compare to base model, human eval, automated metrics.", "medium", ["evaluation"]),
    q("Catastrophic forgetting?", "Model loses general capabilities when fine-tuned too aggressively. Use LoRA, lower lr.", "hard", ["forgetting"]),
    q("OpenAI fine-tuning API?", "Upload JSONL, select base model, train, deploy custom model ID. Simple but less control.", "easy", ["openai"]),
    q("Hyperparameters for LoRA?", "Rank (8-64), alpha, learning rate (1e-4 to 1e-5), epochs (1-3). Start low, increase carefully.", "hard", ["hyperparameters"]),
    q("PEFT methods?", "Parameter-Efficient Fine-Tuning: LoRA, prefix tuning, adapters. Train small fraction of params.", "medium", ["peft"]),
    q("Fine-tuning for safety?", "Include refusal examples, red team data, alignment examples in training set.", "medium", ["safety"]),
    q("Data preparation?", "Clean, deduplicate, balance classes, format consistently, remove PII.", "medium", ["data-prep"]),
    q("Fine-tuning cost?", "LoRA on 7B: ~$10-50. Full fine-tune 70B: $1000+. OpenAI API: per-token training cost.", "medium", ["cost"]),
    q("Serving fine-tuned models?", "Merge LoRA weights into base, or serve adapter separately. vLLM, TGI for inference.", "medium", ["serving"]),
    q("Fine-tuning pipeline?", "Data prep → train → eval → deploy → monitor → iterate. Automate with CI/CD.", "medium", ["pipeline"]),
    q("Common mistakes?", "Too little data, overfitting, wrong format, not evaluating against base model, ignoring safety.", "medium", ["mistakes"]),
    q("Fine-tuning vs distillation?", "Fine-tune: adapt base model. Distill: train smaller model to mimic larger. Different goals.", "medium", ["distillation"]),
  ],
};

export function getInterviewQuestions(slug: string): InterviewQuestion[] {
  return interviewQuestionBank[slug] ?? [];
}
